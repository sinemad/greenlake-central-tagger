version: "3.9"

services:
  greenlake-central-tagger:
    ports:
      - "8000:8000"
    volumes:
      - ./app:/usr/src/app/app
    environment:
      - PYTHONUNBUFFERED=1
    command: ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]
